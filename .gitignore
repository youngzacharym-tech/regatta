node_modules/
stage/node_modules/
stage/dist/
*.log
.env
.env.*
.DS_Store
Thumbs.db
.vscode/
.idea/

# Uncompressed GLB backups (compress-glb.ps1 writes here)
backups/

# api/ws.js is COMMITTED (pre-bundled) so git-based Vercel deploys work —
# regenerate with `npm run build:api` whenever api/ws.ts changes.

.vercel
.env*

# Agent tooling dropped by the Upstash installer — not part of the game
.agents/
.claude/
skills-lock.json
.env*.local

# Pipeline outputs and local Blender scratch
tools/out/
myrmblender/

# Playwright MCP screenshots (session artifacts)
.playwright-mcp/
